
import { ActionComponent } from "../../components/action-components";
import { Gun } from "./gun";

export class GunActionCompoment implements ActionComponent {
    public update(gun: Gun): void {
        
    }
}
